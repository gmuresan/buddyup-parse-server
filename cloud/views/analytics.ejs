
<!DOCTYPE html>
<html>
  <head>
    <title>Sample App</title>
  </head>
  <body>
    <h1>Analytics</h1>
    <form method="post" action="/logout">
          <input type="submit" value="Logout">
    <span> Total Users: <%=numUsers %> </span>
    <ul>
      <% users.forEach(function(user, index, usersToShow) { %>
        <li> <%= user.get("firstName") %> <%= user.get("lastName") %> &nbsp; <%=user.createdAt %> </li>
      <% }); %>
    </ul>
  </body>
</html>